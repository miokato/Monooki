version: '3'
services:
  redis:
    image: redis
    ports:
      - "6379:6379"

  postgres:
    image: postgres
    environment:
      - POSTGRES_DB=monooki
      - POSTGRES_USER=postgres
    ports:
      - "5432:5432"
